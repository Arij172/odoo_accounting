from odoo import models

class AccountAnalyticLineInherited(models.Model):
    _inherit = 'account.analytic.line'  # Héritage du modèle 'account.analytic.line' sans ajout

    # Pas de modifications ou ajouts spécifiques ici
